<resources>

    <style name="MyCustomTheme" parent="AppTheme">

        <!-- CUSTOMIZE IT ACCORDING TO YOUR REQUIREMENT -->
    </style>




    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlHighlight">@color/colorControlHighlight</item>
        <item name="colorControlActivated">@color/colorActivited</item>
        <item name="colorButtonNormal">@color/colorButton</item>


        <!-- For contextual menu -->
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/contextualActionBarStyle</item>
    </style>

    <!-- Contextual Action Bar style -->
    <style name="contextualActionBarStyle" parent="@style/Widget.AppCompat.ActionMode">
        <item name="background">@color/colorControlHighlight</item>
        <item name="titleTextStyle">@style/MyTextStyle</item>
        
        <!-- For defualt back arrow color-->
        <item name="android:actionModeCloseDrawable">@drawable/ic_keyboard_arrow_left</item>
    </style>

    <!-- Contextual ActionBar title text -->
    <style name="MyTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/colorAccent</item>
    </style>


    <!-- Style for the toolbar text -->
    <style name="MyToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- style for the pop up menu -->
    <style name="MyPopUpTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@color/colorActivited</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>
</resources>
